{
  "name": "tableau-comments-app",
  "version": "1.0.0",
  "description": "Tableau Extension for Comments with Snowflake Integration",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.7"
  },
  "engines": {
    "node": ">=18.x"
  }
}