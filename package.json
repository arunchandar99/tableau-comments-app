{
  "name": "tableau-comments-app",
  "version": "1.0.0",
  "description": "Tableau Extension for Comments with Snowflake Integration",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "@vercel/node": "^2.15.10"
  },
  "engines": {
    "node": ">=18.x"
  }
}